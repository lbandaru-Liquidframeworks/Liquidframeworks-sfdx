<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>WeightingFactor__c</fullName>
    <externalId>false</externalId>
    <formula>IF(ISBLANK(TEXT(Ticket_Type__c)), 0, 16) + IF(ISBLANK(Account__c), 0, 8) + IF(ISBLANK(TEXT(Job_Type__c)),0,4) + IF(ISBLANK(Office__c),0,2) + IF(ISBLANK(TEXT(Segment__c)),0,1)</formula>
    <formulaTreatBlanksAs>BlankAsZero</formulaTreatBlanksAs>
    <inlineHelpText>Weighting factor for the checklist.</inlineHelpText>
    <label>WeightingFactor</label>
    <precision>18</precision>
    <required>false</required>
    <scale>2</scale>
    <trackHistory>false</trackHistory>
    <type>Number</type>
    <unique>false</unique>
</CustomField>
